# Changelog Notes

## 0.1.1

- Changed project-scoped generated artifact defaults from `data/downloads/` to `.flameframe/`.
- Added automatic `.flameframe/.gitignore` creation so downloaded media, context packs, segments, and zoom frames are ignored by default.
- Updated docs and built-in manual examples to use `.flameframe/` work directories.
